namespace FactionTactics.Doctrine
{
    /// <summary>Roles assigned by a doctrine pack after discovery.</summary>
    public enum SquadRole
    {
        Unassigned = 0,
        Front,
        Missile,
        Flanker,
        Leader,
    }
}
